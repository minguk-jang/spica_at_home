# Current mode: Codex OAuth through Codex app-server.
#
# Stack on top of base.yaml:
#   python -m webwright.run.cli main -c base.yaml -c model_codex_oauth_text_vision.yaml ...
#
# The default agent uses gpt-5.5. Screenshot-oriented tools use the
# vision_model block when present. This mode reuses the local Codex login
# without spawning nested Codex subprocesses.

model:
  model_class: codex_app_server
  model_name: gpt-5.5
  codex_sandbox: read-only
  request_timeout_seconds: 300
  attach_observation_screenshot: false

vision_model:
  model_class: codex_app_server
  model_name: gpt-5.5
  codex_sandbox: read-only
  request_timeout_seconds: 300
  attach_observation_screenshot: true
